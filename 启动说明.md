# DramaClaw 命令行 CE 本地栈

这套启动方式不依赖 Docker：文本、embedding、语音请求经本地网关转发到硅基流动，图像请求发给
本机 ComfyUI。密钥、机器路径和可编辑工作流都保存在 `.dramaclaw-local/`，该目录已被 Git 忽略，
更新代码不会覆盖它。

## 1. 前置条件

- 已安装 Python 3.11+、`uv`、Node.js 22 与 pnpm 11.5.0。
- 已安装可从命令行启动的 ComfyUI，并确认其目录中存在 `main.py`。
- ComfyUI 已安装工作流使用的节点与模型。至少要能识别 `UnetLoaderGGUF`、
  `TextEncodeQwenImageEditPlus`、`FluxKontextMultiReferenceLatentMethod` 和 `ModelSamplingFlux`。
- 已准备硅基流动 API key。不要把 key 写进本文件、环境示例或任何 tracked 文件。

首次安装依赖：

```bash
uv sync --group dev
cd frontend && pnpm install --frozen-lockfile && cd ..
```

## 2. 创建本机配置

在仓库根目录执行：

```bash
mkdir -p .dramaclaw-local/secrets
cp config/local/local.env.example .dramaclaw-local/local.env
chmod 700 .dramaclaw-local .dramaclaw-local/secrets
chmod 600 .dramaclaw-local/local.env
```

需要由脚本启动 ComfyUI 时，把 `COMFYUI_DIR` 改成当前机器上包含 `main.py` 的目录；路径中有
空格时保留双引号。若 ComfyUI 使用独立 Python，再填写 `COMFYUI_PYTHON`。如果目标
`COMFYUI_BASE_URL` 已经健康，脚本会先复用该进程，此时不要求 `COMFYUI_DIR`。

用文本编辑器把硅基流动 key 单独写入：

```text
.dramaclaw-local/secrets/siliconflow.key
```

文件内容只能是 key 本身，保存后执行：

```bash
chmod 600 .dramaclaw-local/secrets/siliconflow.key
```

首次启动时，网关会自动生成 `router.token`，并把仓库内四份已审查的 ComfyUI API 工作流复制到
`.dramaclaw-local/workflows/`。以后可以修改本机副本；普通代码更新不会覆盖已有副本。

## 3. 启动与停止

在仓库根目录运行：

```bash
bash scripts/start-local-stack.sh
```

默认行为是先探测并复用 `COMFYUI_BASE_URL`；不可用时才按 `COMFYUI_DIR` 启动 ComfyUI，等待
健康检查通过后再启动本地模型网关、DramaClaw API 和前端。要完全跳过 ComfyUI 启动与探测，
可把 `DRAMACLAW_START_COMFYUI` 改为 `0`。

默认端口是 gateway `3001`、API `8780`、前端 `5173`。如果某个默认端口已被 Docker 或其他
进程占用，脚本会选择其后 20 个端口中的第一个空闲值，并在启动日志中打印最终地址；前端代理会
同步指向实际 API 端口。若在环境或 `local.env` 中显式指定了端口，冲突时脚本会直接报出具体端口，
不会悄悄改写操作者的配置。

gateway `3001` 例外：若它已经是使用同一 `.dramaclaw-local` 配置的健康 DramaClaw local gateway，
脚本会直接共用，不会再次 bind；若是当前检出目录遗留且失去健康响应的 gateway，脚本会先正常终止
再重启。其他程序或其他检出目录占用时不会被擅自终止，脚本会提示改端口或人工停止目标进程。

同一 `.dramaclaw-local` 配置目录还会持有一个仅包含 PID 和端口的原子启动锁。重复执行脚本时，
第二个命令会等待第一个实例健康，然后直接报告共用，不会再分别抢占 gateway、API 和前端端口；
进程异常退出遗留的锁会在确认 PID 已失效后自动接管。

按 `Ctrl+C` 停止。本脚本只会终止本次由它启动的网关、DramaClaw 和 ComfyUI；复用的既有
ComfyUI 进程不会被终止。

## 4. 健康检查

另开终端执行：

```bash
curl -fsS http://127.0.0.1:8188/system_stats >/dev/null && echo "ComfyUI OK"
curl -fsS http://127.0.0.1:3001/healthz
curl -fsS http://127.0.0.1:8780/api/v1/config
```

浏览器访问 `http://127.0.0.1:5173`。在本地模式下，图像模型列表只显示 Qwen Image 与
Krea 2 Turbo；标准 CE 启动方式不会显示这两个本机专用选项。
若启动日志显示使用了备用端口，请把上面的 `8780` / `5173` 替换为日志中的实际值。

`/healthz` 返回的四个 workflow 字段都应为 `true`，`siliconflowKey` 也应为 `true`。
若 ComfyUI 报缺模型或缺节点，先在 ComfyUI 中加载 `.dramaclaw-local/workflows/` 对应文件定位，
不要修改 DramaClaw 路由代码来绕过依赖错误。

## 5. 数据与故障定位

- 老检出若已有 `state/local/`，默认继续使用仓库内数据，避免项目看起来“消失”。
- 新安装默认写入 `~/.local/share/dramaclaw-local`；可在 `local.env` 里设置
  `DRAMACLAW_LOCAL_DATA_DIR` 改到其他持久目录。
- 桌面代理不会接管 `127.0.0.1`、`localhost` 或 `::1` 的图片请求；本地图片编辑也不会要求
  OSS relay。
- Krea 2 Turbo Int8 在 macOS 仍属于实验能力，脚本会启用 MPS 的 CPU fallback；速度与质量尚未
  形成跨机器基准，不能据此承诺生产性能。
- 不要提交 `.dramaclaw-local/`、真实 key、本机导出的 community 原始工作流或生成媒体。
