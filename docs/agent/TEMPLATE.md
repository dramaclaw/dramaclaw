# <工作线名称>

> 复制到 `docs/agent/tasks/<slug>.md`，并在 `docs/agent/STATE.md` 登记。
> 同时复制 `docs/agent/CLAIM_TEMPLATE.toml.example` 为 `docs/agent/claims/<slug>.toml`。
> 先完成方案门与 `agent_guard check`，状态才能从
> 「提案中」变为「方案就绪 / 执行中」。完整规则见 `docs/agent/README.md`。

**状态**：提案中
**最后更新**：YYYY-MM-DD
**基线**：`<commit>`；`<branch>` 相对 `<upstream>` ahead N / behind N；工作区相关改动 `<摘要>`
**认领者**：`<人 / agent / 会话标识>`（未认领写「无」）
**相关文档**：`docs/guides/<slug>.md`（无则写「无」）
**相关分支 / PR**：无 ｜ `<branch>` ｜ `#<pr>`

## 目标

一两句话写出用户可感知结果，以及怎样算完成。不要写「优化体验」这种没有终点的话。

## 非目标

- 明确列出本轮不会顺手处理的邻近问题，防止范围漂移。

## 现状与证据

- 已有实现、失败现象、复现步骤、相关提交 / 远端分支。
- 不确定的地方写成待验证假设，不要伪装成事实。
- 记录 `git status`、目标路径 diff 和 `origin/main` / 相关分支的重叠检查结论。

## 写入边界

路径尽量精确到文件。模式只用：`独占` / `共享` / `只读`。共享项必须在「协调与冲突」写清顺序。

| 路径 | 模式 | 作用 / 为什么必须改 |
|---|---|---|
| `path/to/file` | 独占 | |

同一范围同步写入 `docs/agent/claims/<slug>.toml`；台账负责解释，TOML 负责机器拦截。
开始实现前记录 `acquire` / `preflight` 的 owner 与结果，禁止认领仓库根或宽泛源码目录。

## 协调与冲突

- **相关工作线**：无 ｜ `<slug>`
- **本地已有改动**：谁产生、是否能归属；不能归属就停止写入。
- **远端重复实现**：相关分支 / 提交；选择复用、移植、串行合并或放弃，并说明理由。
- **共享文件顺序**：谁先改、接口归谁、谁做最终集成。无共享文件写「无」。

## 实施方案

1. `<步骤>`——完成条件：`<可观察结果>`
2. `<步骤>`——完成条件：`<可观察结果>`

方案变化时先改这里或在进展记录写明分叉，再改代码。

## 风险与回退

- **风险**：可能影响哪些 API、数据、UI、配置、发行边界或兼容性。
- **回退**：如何撤销本工作线，不触碰其他未提交改动。禁止写整树 reset / restore。

## 验收标准

- [ ] `<聚焦测试命令>` → `<期望>`
- [ ] `<静态检查 / 构建命令>` → `<期望>`
- [ ] `<手工路径>` → `<期望>`
- [ ] 本轮改动全部在写入边界内，无未解释 diff；文档、i18n、迁移或合规影响已处理。

## 进展记录

**倒序**，最新在最上面。每条必须回答文件 / 行为、理由、验证；未验证就明确写未验证及原因。

### YYYY-MM-DD · <一句话标题>

做了什么：

为什么这么做（尤其是与原方案分叉的地方）：

怎么验证的（命令 / 界面路径 / 结果）：

## 已定下来的决策

把「已经讨论完、不要再反复推翻」的结论钉在这里，一条一句并附理由。新证据推翻旧决策时，
不要静默改写；保留原决策并标记已废弃，链接到新的进展记录。

- **<决策>**——<理由>

## 待办

- [ ] 具体到下一任可以直接开工；不要写「继续完善」。

## 阻塞

写清代码 / 环境 / 权限 / 用户决策中的哪一种阻塞、缺什么、如何解除。无则写「无」。

## 交接摘要

- **最后完成到**：
- **下一步唯一动作**：
- **先读这些文件**：
- **不要动这些文件 / 决策**：
